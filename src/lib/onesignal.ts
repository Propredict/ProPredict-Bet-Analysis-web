/**
 * OneSignal Web Push is now initialized entirely in index.html.
 * This function is kept as a no-op so existing imports don't break.
 */
export function initOneSignalWeb() {
  // No-op: SDK script + init live in index.html to avoid duplicate initialization.
}
